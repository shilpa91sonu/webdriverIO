import {expect,should} from "chai"
describe("",()=>{
    it("",async()=>{
        const should = require('chai').should()
        (3).should.be.equal.to(3)
        ("3").should.be.equal.to(3)
        ({a:aaa,b:bbb,name:shilpa}).should.have("name")
        [2,3,4,5].should.include(4)
// browser.url("http://49.249.28.218:8081/AppServer/")
// let a = browser.getUrl();
// (a).should.be.equal("http://49.249.28.218:8081/AppServer/")

    })
})